## Goal
Make the home page completely blank — just a plain white page with no content.

## Changes
- Update `src/pages/Index.tsx` to render an empty white full-screen container (no placeholder image, no text).

That's it — the rest of the app (routing, providers) stays untouched.